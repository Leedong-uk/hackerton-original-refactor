<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="backend.kb_hack.domain.member.mapper.ProfileImageMapper">

    <insert id="insertProfileImage">
        INSERT INTO profile_image (member_id, storage_key)
        VALUES (#{memberId}, #{profileImage})
    </insert>
</mapper>